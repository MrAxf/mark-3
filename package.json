{
  "name": "mark-sorcery",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground",
    "docs"
  ],
  "scripts": {
    "build": "bun run build:parser && bun run build:vue",
    "build:parser": "bun run --filter @mark-sorcery/markdown-parser build",
    "build:vue": "bun run --filter @mark-sorcery/vue build",
    "fmt": "bunx oxfmt .",
    "lint": "bunx oxlint .",
    "playground": "bun run --filter playground dev",
    "publish:parser": "bun run --filter @mark-sorcery/markdown-parser prepublishOnly && cd packages/markdown-parser && bun publish",
    "publish:vue": "bun run --filter @mark-sorcery/vue prepublishOnly && cd packages/vue && bun publish",
    "test": "bun run --filter @mark-sorcery/markdown-parser test && bun run --filter @mark-sorcery/vue test"
  },
  "devDependencies": {
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "oxlint-tsgolint": "^0.16.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "bun@1.3.10"
}
