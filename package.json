{
  "name": "mark-sorcery",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground",
    "docs"
  ],
  "scripts": {
    "build": "bun run build:parser && bun run build:classes && bun run build:vue",
    "build:classes": "bun run --filter @mark-sorcery/plugin-add-classes build",
    "build:parser": "bun run --filter @mark-sorcery/markdown-parser build",
    "build:vue": "bun run --filter @mark-sorcery/vue build",
    "fmt": "bun oxfmt .",
    "lint": "bun oxlint .",
    "playground": "bun run --filter playground dev",
    "publish:classes": "bun run --filter @mark-sorcery/plugin-add-classes prepublishOnly && cd packages/plugin-add-classes && bun publish",
    "publish:parser": "bun run --filter @mark-sorcery/markdown-parser prepublishOnly && cd packages/markdown-parser && bun publish",
    "publish:vue": "bun run --filter @mark-sorcery/vue prepublishOnly && cd packages/vue && bun publish",
    "test": "bun run test:classes && bun run test:parser && bun run test:vue",
    "test:classes": "bun run --filter @mark-sorcery/plugin-add-classes test",
    "test:parser": "bun run --filter @mark-sorcery/markdown-parser test",
    "test:vue": "bun run --filter @mark-sorcery/vue test"
  },
  "devDependencies": {
    "oxfmt": "0.40.0",
    "oxlint": "1.55.0",
    "oxlint-tsgolint": "0.16.0",
    "vitest": "4.1.0"
  },
  "packageManager": "bun@1.3.10"
}
